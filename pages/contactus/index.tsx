import * as React from 'react';

interface IContactUsProps {
}

const ContactUs: React.FunctionComponent<IContactUsProps> = (props:any) => {
  return (
    <div>
        
    </div>
  );
};

export default ContactUs;
